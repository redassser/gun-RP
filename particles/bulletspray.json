{
    "format_version": "1.10.0",
    "particle_effect": {
      "description": {
        "identifier": "rr:bulletspray",
        "basic_render_parameters": {
          "material": "particles_alpha",
          "texture": "textures/particle/bullethole"
        }
      },
      "components": {
        "minecraft:emitter_rate_instant": {
          "num_particles": "variable.intensity * 3"
        },
        "minecraft:emitter_lifetime_expression": {
          "activation_expression": "(variable.emitter_age > 0 ? 1.0 : 0.0)",
          "expiration_expression": "(variable.emitter_age > 10.0 ? 1.0 : 0.0)"
        },
        "minecraft:emitter_shape_point": {
          "direction": [
            "variable.direction.x * Math.random(2,5) + Math.random(-1,1)",
            "variable.direction.y * Math.random(2,5) + Math.random(-1,1)",
            "variable.direction.z * Math.random(2,5) + Math.random(-1,1)"
          ]
        },
        "minecraft:particle_motion_collision": {
          "coefficient_of_restitution": 1.0,
          "collision_drag": 0,
          "collision_radius": 0.1
        },
        "minecraft:particle_initial_speed": "(1.0 + Math.random(0, 1) + Math.random(0, 1))",
        "minecraft:particle_lifetime_expression": {
          "max_lifetime": "7"
        },
        "minecraft:particle_initial_spin": {
          "rotation": "Math.random(-180,180)"
      },
        "minecraft:particle_motion_dynamic": {
          "linear_acceleration": [ 0.0, "1 * (-3)", 0.0 ],
          "linear_drag_coefficient": 0
        },
        
        "minecraft:particle_appearance_billboard": {
          "size": [ "0.1", "0.1" ],
          "facing_camera_mode": "rotate_xyz"
        }
      }
    }
  }